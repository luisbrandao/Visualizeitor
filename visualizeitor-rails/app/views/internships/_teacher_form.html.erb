<%= form_for(@internship) do |f| %>

  <div class="field">
    <%= f.label :assistType, "Tipo de supervisão" %><br />
    <%= f.select :assistType, [["Direta", 0], ["Semi-direta", 1], ["Indireta", 2]] %>
  </div>

  <div class="actions">
    <%= f.submit "Assumir supervisão" %>
  </div>
<% end %>
