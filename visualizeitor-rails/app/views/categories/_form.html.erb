<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descr, "Descrição" %><br>
    <%= f.text_field :descr %>
  </div>
  <div class="field">
    <%= f.label :softlimit, "Horas por semestre" %><br>
    <%= f.number_field :softlimit %>
  </div>
  <div class="field", "Maximo de horas">
    <%= f.label :hardlimit %><br>
    <%= f.number_field :hardlimit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
